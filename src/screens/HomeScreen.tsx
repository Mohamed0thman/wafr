import {StyleSheet, Text, View} from "react-native";
import React from "react";
import {RootScreen, TextField} from "../components";

const HomeScreen = () => {
  return <RootScreen></RootScreen>;
};

export default HomeScreen;

const styles = StyleSheet.create({});
